import 'package:flutter/material.dart';

class AppColours {
  AppColours._();

  // Primary brand colours
  static const teal = Color(0xFF18E7B6);
  static const deepNavy = Color(0xFF081430);

  // Functional palette
  static const darkNavy1 = Color(0xFF0A2855);
  static const lightWhite = Color(0xFFFFFFFF);
  static const lightOffWhite = Color(0xFFF2F2F2);

  // Accents & UI
  static const accentDeepBlue = Color(0xFF0B1D44);
  static const accentSoftTeal = Color(0xFF56EFCC);
  static const accentMutedNavy = Color(0xFF1B2E59);
  static const accentCoolGrey = Color(0xFFE1E5EE);
  static const accentBlack = Color(0xFF000000);

  // Links
  static const hyperlink = Color(0xFF18E7B6);
  static const followedHyperlink = Color(0xFF3A4B71);
}
